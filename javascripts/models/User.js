var User;

User = (function() {
  function User(fullName, email, bestPony, avatar) {
    this.fullName = fullName;
    this.email = email;
    this.bestPony = bestPony;
    this.avatar = avatar;
  }

  return User;

})();
